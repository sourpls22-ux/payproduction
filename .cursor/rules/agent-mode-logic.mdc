# Agent Mode Decision Logic

## 🎯 Критерий принятия решения
**ОСНОВНОЕ ПРАВИЛО**: Если изменение будет **потеряно при следующем `git pull`**, то ОБЯЗАТЕЛЬНО предлагать Agent Mode.

## 📋 Точные фразы для использования

### ⚠️ Когда изменение будет потеряно при `git pull`:
```
🔧 Решение:
⚠️ Это изменение будет потеряно при следующем git pull при прямом изменении на VPS, нужно изменить локально и запушить в GitHub ⚠️

Переключите меня в режим агента, и я:
✅ Исправлю [описание задачи]
✅ Убеждусь, что [детали исправления]
✅ Закоммичу исправления в GitHub
✅ Дам команды для обновления на VPS

Переключайте в режим агента! 🚀
```

### ✅ Когда изменение НЕ будет потеряно:
```
✅ **Это изменение сохранится на VPS**

Выполните команды на VPS:
```bash
[команды]
```

Это изменение не будет потеряно при обновлениях.
```

### 🤔 Для диагностики и анализа:
```
🔍 **Для диагностики нужен анализ кода**

Переключите меня в режим агента, и я:
- Проанализирую код локально
- Найду причину проблемы
- Предложу правильное решение
```

### 🤔 Для неопределенных случаев:
```
🤔 **Нужно проверить, сохранится ли это изменение**

Переключите меня в режим агента, и я:
- Проанализирую, что именно нужно изменить
- Определю, где лучше сделать изменение (локально или на VPS)
- Выполню изменение правильным способом
```

## 📝 Логика принятия решения

### ✅ ОБЯЗАТЕЛЬНО предлагать Agent Mode для:
- **Изменения в коде** (`.js`, `.jsx`, `.ts`, `.tsx`, `.json`, `.config.js`)
- **Конфигурационные файлы** (`.gitignore`, `package.json`, `vite.config.js`)
- **Статические файлы** (HTML, CSS, изображения, шрифты)
- **Документация** (`.md` файлы)
- **Структура проекта** (создание/удаление файлов/папок)
- **Любые изменения, которые должны сохраниться в Git**
- **Анализ кода** и поиск проблем
- **Исправление ошибок** в файлах

### ⚠️ НЕ предлагать Agent Mode для:
- **Временные изменения** (перезапуск сервисов, проверка статуса)
- **Системные команды** (`pm2 restart`, `git pull`, `npm install`)
- **Проверка логов** и диагностика
- **Настройка окружения** (переменные `.env` с секретами)
- **Команды для обновления** на VPS

## 🔄 Алгоритм принятия решения

1. **Вопрос**: "Будет ли это изменение потеряно при `git pull`?"
2. **Если ДА** → Использовать фразу "⚠️ Это изменение будет потеряно..."
3. **Если НЕТ** → Дать команды для VPS
4. **Если НЕ УВЕРЕН** → Использовать фразу "🤔 Нужно проверить..."
5. **Для диагностики** → Использовать фразу "🔍 Для диагностики нужен анализ..."

## 🎯 Цель правила
Обеспечить, чтобы все изменения, которые должны сохраниться в проекте, делались локально и коммитились в GitHub, а не терялись при обновлениях на VPS.